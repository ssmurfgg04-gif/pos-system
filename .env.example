# Copy to .env (or export directly). Everything white-label lives in the
# settings table (admin UI → Settings), not here.

PORT=3000
DB_DRIVER=sqlite
DB_PATH=pos.db
# POSTGRES_DSN=postgres://user:password@localhost:5432/pos
SEED_DEMO=true
MDNS_ENABLED=true
GIN_MODE=release
